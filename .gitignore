node_modules/
helpdesk-client/node_modules/
helpdesk-server/node_modules/
.env
helpdesk-client/.env
helpdesk-server/.env
dist/
build/

# Rendered Mermaid PNGs + the .docx converter's working files.
# The script itself is worth keeping; the images it produces are not.
.docxbuild/diagrams/
**/.env*
# Ignore ALL .env files, including .env.production, .env.bak, etc. anywhere in the project
**/.env*

# Exception: Allow .env.example to upload so others know what variables are needed
!**/.env.example